// Re-exported from context/AuthContext.jsx (the canonical location per the project's
// required structure) so existing `import { AuthContext } from '../AuthContext'`
// call sites keep working without a mechanical rename of every consumer.
export { AuthContext, AuthProvider } from './context/AuthContext';
